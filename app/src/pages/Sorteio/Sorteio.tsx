import { Input } from "../../components"

export const Sorteio = () => {
    return (
        <main className="secreto">
            <ul className="secreto__list">
                <Input name="jogador[0]" label="Jogador 1"/>
            </ul>
        </main>
    )
}